import React, { useState, useEffect } from 'react';
import axios from 'axios';

export const Bookmark = () => {
  const { bookmarkedNews, removeBookmark } = useBookmarks();

    return (
<div> hello</div>
    );
};